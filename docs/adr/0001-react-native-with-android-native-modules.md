# 使用 React Native 与 Android 原生模块构建 MVP

MVP 采用 React Native 承载主要界面和业务编排，使用 Android 原生模块实现悬浮控制、屏幕捕获、后台运行和系统级覆盖提示。这样可以保留 JavaScript/TypeScript 的跨端开发体验，同时承认当前核心能力主要依赖 Android 系统权限和原生 API；iOS 与其他平台暂不作为 MVP 的实现目标。
